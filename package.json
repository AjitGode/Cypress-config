{
  "name": "cypress8am",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"npx cypress run --browser new_chrome --headed",
    "local":"npx cypress run --headed --config-file local-config.json",
    "staging":"npx cypress run --headed --config-file staging-config.json",
    "production":"npx cypress run --headed --config-file production-config.json",
    "canary":"npx cypress run --headed --config-file canary-config.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.7.0",
    "npx": "^10.2.2"
  }
}
